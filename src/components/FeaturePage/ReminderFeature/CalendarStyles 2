.custom-calendar .react-calendar {
    border: none;
    border-radius: 10px;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
    font-family: 'Arial', sans-serif;
    background-color: #1c1c1c;
    color: #fff;
    padding: 50px; /* Increased padding for overall calendar */
    width: 100%; /* Full width for better proportion */
    max-width: 500px; /* Maximum width for larger screens */
    margin: auto; /* Center the calendar */
  }
  
  .custom-calendar .react-calendar__tile {
    max-width: 100%;
    padding: 10px; /* Consistent padding for each tile */
    margin: 4px; /* Space between tiles */
    background-color: #2a2a2a;
    border-radius: 8px;
    text-align: center;
    color: #dcdcdc;
    flex-grow: 1; /* Flex grow for proportional sizing */
  }
  
  .custom-calendar .react-calendar__navigation {
    display: flex;
    justify-content: space-between; /* Align navigation buttons */
    margin-bottom: 15px; /* Margin below the navigation buttons */
  }
  
  .custom-calendar .react-calendar__navigation button {
    background-color: #343434;
    font-size: 1.2em;
    color: white;
    border: none;
  }
  
  .custom-calendar .react-calendar__month-view__weekdays {
    text-align: center;
    padding: 5px 0;
    margin-bottom: 10px;
  }
  
  .custom-calendar .react-calendar__month-view__weekdays__weekday {
    flex-grow: 1; /* Flex grow for proportional weekdays */
  }
  
  .custom-calendar .react-calendar__month-view__days {
    justify-content: space-between; /* Space between day columns */
  }
  
  /* Continue with other styles as previously defined */
  